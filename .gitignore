# secrets / runtime state
.env
*.session
*.session-journal
*.db
*.sqlite*
data/

# `review` proposals: scratch until applied, and the applied change is the git record
profiles/reviews/

# research scratch (superseded by *.jsonl corpus)
research/samples-*/*.html
research/samples-*/sim_*.txt

# python
__pycache__/
*.pyc
.venv/
config.local.yaml

# macOS
.DS_Store
