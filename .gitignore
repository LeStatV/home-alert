# secrets / runtime state
.env
*.session
*.session-journal
*.db
*.sqlite*
data/

# `review` proposals: scratch until applied, and the applied change is the git record
profiles/reviews/

# research scratch (superseded by *.jsonl corpus)
research/samples-*/*.html
research/samples-*/sim_*.txt

# python
__pycache__/
*.pyc
.venv/

# per-deployment wiring: copy the committed *.example.* files and edit the copies
config.yaml
config.local.yaml
docker-compose.yml

# macOS
.DS_Store
